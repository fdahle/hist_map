version: '3.8'

services:
  # Backend server with preprocessing capabilities
  server:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: stratum3D_server
    ports:
      - "3000:3000"
    volumes:
      # Mount input folder for preprocessing
      - ./input:/app/input:ro
      # Mount data folder to persist processed files
      - ./server/data:/app/server/data
    env_file:
      - path: .env
        required: false
    environment:
      - NODE_ENV=production
      # Set ADMIN_ENABLED=false in your root .env to strip the admin panel entirely.
      - ADMIN_ENABLED=${ADMIN_ENABLED:-true}
    command: node index.js
    networks:
      - stratum3D_network

  # Frontend client (production build with nginx)
  client:
    build:
      context: ./client
      dockerfile: Dockerfile
      args:
        VITE_DEFAULT_THEME: ${VITE_DEFAULT_THEME:-auto}
        VITE_API_URL: ${VITE_API_URL:-http://localhost:3000}
    container_name: stratum3D_client
    ports:
      - "8080:80"
    depends_on:
      - server
    networks:
      - stratum3D_network

  # Alternative: Development mode for client (hot reload)
  client-dev:
    image: node:20-alpine
    container_name: stratum3D_client_dev
    working_dir: /app
    ports:
      - "5173:5173"
    volumes:
      - ./client:/app
      - /app/node_modules
    command: sh -c "npm install && npm run dev -- --host"
    profiles:
      - dev
    networks:
      - stratum3D_network

networks:
  stratum3D_network:
    driver: bridge
